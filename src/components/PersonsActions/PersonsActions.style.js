export const style = (theme) => ({
  container: {
    padding: theme.spacing(1),
  },
  divider: {
    marginInline: theme.spacing(1),
  },
  breadcrumbs: {
    flexGrow: "1",
    display: "flex",
  },
  searchFilter: {
    marginTop: 5,
  },
});
